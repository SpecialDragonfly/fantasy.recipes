; Tuned conservatively for the Pi's limited memory. validate_timestamps=0
; means PHP won't notice source changes until php-fpm restarts -- which the
; deploy flow (`docker compose up -d --build`) already does on every deploy.
opcache.enable=1
opcache.memory_consumption=64
opcache.max_accelerated_files=10000
opcache.validate_timestamps=0
